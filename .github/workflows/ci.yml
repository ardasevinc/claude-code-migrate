name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - name: Check out repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.3.14
      - name: Install ShellCheck on macOS
        if: runner.os == 'macOS'
        run: brew install shellcheck
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Run full gate
        run: bun run check

  package_smoke:
    name: Package smoke
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.3.14
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Pack once and smoke exact tarball
        shell: bash
        run: |
          tarball=$(npm pack --pack-destination "$RUNNER_TEMP" --silent)
          bun scripts/smoke-package.ts "$RUNNER_TEMP/$tarball"

  required:
    name: Required
    if: always()
    needs: [test, package_smoke]
    runs-on: ubuntu-latest
    steps:
      - name: Confirm required jobs passed
        env:
          TEST_RESULT: ${{ needs.test.result }}
          PACKAGE_SMOKE_RESULT: ${{ needs.package_smoke.result }}
        run: |
          test "$TEST_RESULT" = success
          test "$PACKAGE_SMOKE_RESULT" = success
